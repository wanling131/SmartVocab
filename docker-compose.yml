# 本地/演示编排：应用 + MySQL（生产请使用托管数据库与独立密钥管理）
services:
  db:
    image: mysql:8.0
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-smartvocab_root}
      MYSQL_DATABASE: ${DB_NAME:-smartvocab}
    ports:
      - "3306:3306"
    volumes:
      - smartvocab_mysql:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      SECRET_KEY: ${SECRET_KEY:-please-set-secret-key-in-production}
      DB_HOST: db
      DB_USER: root
      DB_PASSWORD: ${DB_PASSWORD:-smartvocab_root}
      DB_NAME: ${DB_NAME:-smartvocab}
      CORS_ORIGINS: ${CORS_ORIGINS:-http://localhost:5000}
    depends_on:
      db:
        condition: service_healthy

volumes:
  smartvocab_mysql:
